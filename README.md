# Crime-Classifier
This project uses K-Nearest Neighbours (KNN) to predict crime type by comparing new incidents to similar historical ones. Model performance is validated on a test set of unseen data
